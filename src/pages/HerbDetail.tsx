
import Layout from "@/components/Layout";
import HerbDetailContent from "@/components/HerbDetailContent";

const HerbDetail = () => {
  return (
    <Layout>
      <HerbDetailContent />
    </Layout>
  );
};

export default HerbDetail;
